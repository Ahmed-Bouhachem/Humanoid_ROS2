# Environment variables for docker-compose
# Copy this file to .env and modify as needed:
#   cp .env.example .env

# Compose project name shown by `docker compose ps`
COMPOSE_PROJECT_NAME=grove-g1

# ROS distribution (jazzy, humble, iron, etc.)
ROS_DISTRO=jazzy

# Upstream base image: <BASE_IMAGE_REPO>:<ROS_DISTRO><BASE_IMAGE_TAG_SUFFIX>
BASE_IMAGE_REPO=adyansh04/ros-dev
BASE_IMAGE_TAG_SUFFIX=-latest

# Derived dev image built from .devcontainer/Dockerfile
DEV_IMAGE_REPO=adyansh04/ros-dev-workspace

# Container naming prefix: <CONTAINER_PREFIX>_<ROS_DISTRO>
CONTAINER_PREFIX=ros_dev

# Display for GUI applications (usually inherited from host)
# DISPLAY=:0

# --- Pointing the container at a real G1 -------------------------------------
# Unset, these default to the simulation values: domain 1 and the loopback CycloneDDS profile,
# which is what keeps the simulator's rt/lowcmd off the LAN. g1_bringup/sim.launch.py refuses
# to start unless the profile in use pins the 'lo' interface, so the simulator cannot be brought
# up against a robot by accident.
#
# GROVE_G1_ROS_DOMAIN_ID=1
# GROVE_G1_CYCLONEDDS_URI=file:///etc/cyclonedds/cyclonedds.hardware.xml
# GROVE_G1_ROBOT_NIC=eth0
