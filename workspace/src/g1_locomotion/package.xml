<?xml version="1.0"?>
<?xml-model href="http://download.ros.org/schema/package_format3.xsd" schematypens="http://www.w3.org/2001/XMLSchema"?>
<package format="3">
  <name>g1_locomotion</name>
  <version>0.1.0</version>
  <description>
    Base approach for the Unitree G1: closes the last half metre to an object under velocity
    control, as the ApproachObject and Retreat actions. See README for the control law.
  </description>
  <maintainer email="gupta.adyansh@gmail.com">Adyansh</maintainer>
  <license>BSD-3-Clause</license>

  <buildtool_depend>ament_cmake</buildtool_depend>

  <depend>rclcpp</depend>
  <depend>rclcpp_action</depend>
  <depend>geometry_msgs</depend>
  <depend>g1_msgs</depend>

  <!-- It closes on an object it reads off /objects, which is why a locomotion package depends
       on a perception message type: the alternative is a manipulation package publishing
       velocity, and every /cmd_vel writer belongs here. -->
  <depend>vision_msgs</depend>
  <depend>tf2</depend>
  <depend>tf2_ros</depend>
  <depend>tf2_geometry_msgs</depend>

  <!-- Lint checks run via workspace-root CTests (see CMakeLists.txt). -->
  <test_depend>ament_cmake_gmock</test_depend>
  <test_depend>ament_cmake_lint_cmake</test_depend>
  <test_depend>ament_cmake_xmllint</test_depend>
  <test_depend>ament_lint_auto</test_depend>

  <export>
    <build_type>ament_cmake</build_type>
  </export>
</package>
